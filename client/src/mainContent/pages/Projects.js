import React from "react"


// Here we will do the call to the Mongo dB data base, and we will print on the screen the challenge that was retrive
// You will do a call to a endpoint that, when it get to the server, the server , will do the call to the database to retrice the data that it
// needs so all the logic will happen behing the since, they only think that will happen here is
// do a call to an endpoint with the useeffect, and the console.log what is received

const Projects = () => {

    return <h1>Curious</h1>
}

export default Projects;